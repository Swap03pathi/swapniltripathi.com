/** Canonical slug + paths for Multidimensional Market Intelligence Engine case study. */
export const MMIE_PROJECT_SLUG = 'multidimensional-market-intelligence-engine';

export function mmieProjectPath() {
  return `/project/${MMIE_PROJECT_SLUG}`;
}

export function mmieArchitecturePath() {
  return `/project/${MMIE_PROJECT_SLUG}/architecture`;
}
