/** Canonical slug + paths for the Real-Time Virtual Execution case study. */
export const RTE_PROJECT_SLUG = 'real-time-virtual-execution-system';

export function rteProjectPath() {
  return `/project/${RTE_PROJECT_SLUG}`;
}

export function rteArchitecturePath() {
  return `/project/${RTE_PROJECT_SLUG}/architecture`;
}
