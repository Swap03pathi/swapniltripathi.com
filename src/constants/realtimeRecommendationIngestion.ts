/** Canonical slug + paths for Realtime Recommendation Ingestion case study. */
export const RRIS_PROJECT_SLUG = 'realtime-recommendation-ingestion-system';

export function rrisProjectPath() {
  return `/project/${RRIS_PROJECT_SLUG}`;
}

export function rrisArchitecturePath() {
  return `/project/${RRIS_PROJECT_SLUG}/architecture`;
}
